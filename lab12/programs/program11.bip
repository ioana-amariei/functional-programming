func suma(a,b)
  if b == 0 then {
    result := a;
  } else {
    result := suma(a + 1, b - 1);
  }
  return result

func main()
  x := 1000;
  y := 0;
  while x > 0
  {
    y := suma(y, x);
    x := x - 1;
  }
  return y

func square(a)
  return a * a
